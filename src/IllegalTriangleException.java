import java.util.InputMismatchException;

public class IllegalTriangleException {

    /*public void setEdad(int edad) {
        if (side1 + side2> side3 || edad > 150) {
            throw new IllegalArgumentException("Triángulo infrige la regla.");
        }
        this.edad = edad;
    }
*/
}
